<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upgrade To Admin</title>
</head>
<body>
    <h1>Upgrade To Admin</h1>
    <h2>Enter secrect password to become admin of this website</h2>
    <form action="/upgradeAccount/admin" method="post">
        <label for="secrectPassword">Secrect password: </label>
        <input name="secrectPassword" type="password" placeholder="Enter secrect password here ...">
        <button type="submit">Upgrade to admin</button>
    </form>
    <% if (errors && errors.length > 0) { %>
        <% errors.forEach(error => { %>
        <li>
            <%= error.msg  %>
        </li>
        <% }) %>
    <% } %>
</body>
</html>